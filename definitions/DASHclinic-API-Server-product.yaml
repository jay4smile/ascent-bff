product: '1.0.0'

info:
  name: dashclinic-api-server
  title: DASHclinic-API-Server
  version: '1.0.0'

apis:
  'dashclinic-api-server':
    $ref: DASHclinic-API-Server.yaml

visibility:
  view:
    type: public
  subscribe:
    type: authenticated

plans:
  default:
    title: Default Plan
    description: Default Plan
    approval: false
    rate-limit:
      value: 100/hour
      hard-limit: false
